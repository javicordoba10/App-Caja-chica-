import 'dart:async';
import 'dart:typed_data';

class PdfConverter {
  static Future<String> convertPdfToImage(String pdfPath) async {
    throw UnimplementedError('PDF conversion is not supported on this platform.');
  }
}
